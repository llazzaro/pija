.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install pija

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pija
    $ pip install pija
